name: Update Leaderboard

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-leaderboard:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Download benchmark artifacts
      uses: actions/download-artifact@v4
      with:
        path: benchmark-artifacts
        pattern: 'benchmark-outputs-*'
        merge-multiple: true
        
    - name: Prepare results
      run: |
        mkdir -p results
        find benchmark-artifacts -name result.json -exec sh -c 'user_dir=$(basename $(dirname $1) | sed "s/benchmark-outputs-//"); mkdir -p "results/$user_dir"; cp "$1" "results/$user_dir/result.json"' sh {} \;
        
    - name: Generate leaderboard
      run: |
        python3 leaderboard.py
        cat LEADERBOARD.md
        
    - name: Commit and push
      run: |
        git config user.name "github-actions"
        git config user.email "actions@users.noreply.github.com"
        git add LEADERBOARD.md
        git commit -m "📊 Обновление таблицы лидеров"
        git push
