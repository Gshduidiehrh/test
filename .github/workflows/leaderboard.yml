name: Update Leaderboard

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-leaderboard:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts
        pattern: '*-result'
        merge-multiple: true
      
    - name: Prepare results directory
      run: |
        # Всегда создаем каталоги
        mkdir -p artifacts leaderboard_results results
        
        # Проверяем есть ли артефакты
        if [ -n "$(find artifacts -type f)" ]; then
          echo "Обработка артефактов..."
          cd artifacts
          for file in *; do
            if [ -f "$file" ]; then
              username="${file%-result}"
              echo "Обработка: $file → Пользователь: $username"
              mkdir -p "../leaderboard_results/$username"
              mv "$file" "../leaderboard_results/$username/result.json"
            fi
          done
          cd ..
        else
          echo "Артефактов не найдено"
        fi
        
        # Копируем обработанные данные
        cp -R leaderboard_results/* results/ || echo "Нет данных для копирования"
        
        echo "Финальная структура results:"
        find results -type f || echo "Каталог results пуст"
        
    - name: Generate leaderboard
      run: |
        python3 leaderboard.py
        cat LEADERBOARD.md
        
    - name: Commit and push
      run: |
        git config user.name "github-actions"
        git config user.email "actions@users.noreply.github.com"
        git add LEADERBOARD.md
        
        if [ -n "$(git status --porcelain)" ]; then
          git commit -m "📊 Automated leaderboard update"
          git push
        else
          echo "No changes to commit"
        fi
